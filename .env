# Application Configuration
APP_NAME="Options Dashboard API"
VERSION="1.0.0"
DEBUG=True

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Data Directories
# For shared data with Flask app, use: ../options-dashboard/live_data
# For separate data, use: ./live_data
LIVE_DATA_DIR=./live_data

# Processing Configuration (NOT USED - processing is now on-demand via upload API)
# REFRESH_INTERVAL is obsolete - no background processing
# AUTO_PROCESS is obsolete - upload API triggers processing
REFRESH_INTERVAL=6
AUTO_PROCESS=True
